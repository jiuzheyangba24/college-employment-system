<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.student.app.mapper.StudentMapper">
    
    <resultMap id="BaseResultMap" type="Student">
        <id column="id" property="id"/>
        <result column="snum" property="snum"/>
        <result column="name" property="name"/>
        <result column="tel" property="tel"/>
        <result column="sex" property="sex"/>
        <result column="major_id" property="majorId"/>
        <result column="grade" property="grade"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="major_name" property="majorName"/>
        <result column="department_name" property="departmentName"/>
    </resultMap>
    
    <select id="findAll" resultMap="BaseResultMap">
        SELECT s.*, m.name as major_name, d.name as department_name
        FROM students s
        LEFT JOIN majors m ON s.major_id = m.id
        LEFT JOIN departments d ON m.department_id = d.id
        ORDER BY s.id
    </select>
    
    <select id="findByPage" resultMap="BaseResultMap">
        SELECT s.*, m.name as major_name, d.name as department_name
        FROM students s
        LEFT JOIN majors m ON s.major_id = m.id
        LEFT JOIN departments d ON m.department_id = d.id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (s.snum LIKE CONCAT('%', #{keyword}, '%') OR s.name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY s.id
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM students s
        <where>
            <if test="keyword != null and keyword != ''">
                AND (s.snum LIKE CONCAT('%', #{keyword}, '%') OR s.name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>
    
    <select id="findById" resultMap="BaseResultMap">
        SELECT s.*, m.name as major_name, d.name as department_name
        FROM students s
        LEFT JOIN majors m ON s.major_id = m.id
        LEFT JOIN departments d ON m.department_id = d.id
        WHERE s.id = #{id}
    </select>
    
    <select id="findBySnum" resultMap="BaseResultMap">
        SELECT s.*, m.name as major_name, d.name as department_name
        FROM students s
        LEFT JOIN majors m ON s.major_id = m.id
        LEFT JOIN departments d ON m.department_id = d.id
        WHERE s.snum = #{snum}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO students (snum, name, tel, sex, major_id, grade, status)
        VALUES (#{snum}, #{name}, #{tel}, #{sex}, #{majorId}, #{grade}, #{status})
    </insert>
    
    <update id="update">
        UPDATE students
        SET snum = #{snum}, name = #{name}, tel = #{tel}, sex = #{sex}, 
            major_id = #{majorId}, grade = #{grade}, status = #{status}
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM students WHERE id = #{id}
    </delete>
    
    <insert id="batchInsert">
        INSERT INTO students (snum, name, tel, sex, major_id, grade, status)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.snum}, #{item.name}, #{item.tel}, #{item.sex}, #{item.majorId}, #{item.grade}, #{item.status})
        </foreach>
    </insert>
    
    <select id="countByStatus" resultType="java.util.Map">
        SELECT status as name, COUNT(*) as value FROM students GROUP BY status
    </select>
    
    <select id="countByMajor" resultType="java.util.Map">
        SELECT m.name as name, COUNT(s.id) as value
        FROM students s
        LEFT JOIN majors m ON s.major_id = m.id
        GROUP BY m.name
    </select>
</mapper>
